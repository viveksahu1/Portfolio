import { defineComponent, shallowRef, onMounted, openBlock, createBlock, Teleport, renderSlot, createCommentVNode } from 'vue';

const _sfc_main = /* @__PURE__ */ defineComponent({
  __name: "Portal",
  props: {
    to: { default: "body" },
    forceMount: { type: Boolean }
  },
  setup(__props) {
    const isMounted = shallowRef(false);
    onMounted(() => {
      isMounted.value = true;
    });
    return (_ctx, _cache) => {
      return isMounted.value || _ctx.forceMount ? (openBlock(), createBlock(Teleport, {
        key: 0,
        to: _ctx.to
      }, [
        renderSlot(_ctx.$slots, "default")
      ], 8, ["to"])) : createCommentVNode("", true);
    };
  }
});

export { _sfc_main as _ };
//# sourceMappingURL=portal.mjs.map
